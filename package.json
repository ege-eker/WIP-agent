{
  "name": "agentic-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w packages/backend\" \"npm run dev -w packages/frontend\"",
    "build": "npm run build -w packages/backend && npm run build -w packages/frontend",
    "ingest": "npm run ingest -w packages/backend",
    "seed": "tsx scripts/seed-test-documents.ts"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.17.4",
    "concurrently": "^8.2.2",
    "docx": "^9.5.1",
    "pdf-parse": "^1.1.1",
    "pdfkit": "^0.17.2",
    "tsx": "^4.19.0",
    "typescript": "^5.5.3"
  }
}
